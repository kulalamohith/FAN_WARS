version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: warzone-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Note: PostgreSQL is included here for staging/production parity.
  # For local MVP development on machines without Docker Desktop,
  # the Prisma schema is currently configured to use local SQLite (dev.db).
  postgres:
    image: postgres:15-alpine
    container_name: warzone-postgres
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: warzone
      POSTGRES_PASSWORD: warzone
      POSTGRES_DB: warzone
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U warzone"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  redis_data:
  postgres_data:
